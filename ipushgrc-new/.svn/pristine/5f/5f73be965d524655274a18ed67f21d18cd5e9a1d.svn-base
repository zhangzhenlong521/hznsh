package com.pushworld.ipushgrc.ui.bsd.otherbsd;

import java.awt.BorderLayout;

import cn.com.infostrategy.ui.common.AbstractWorkPanel;
import cn.com.infostrategy.ui.sysapp.dictmanager.ComboxDictManagerPanelNew;

/**
 * 其他所有下拉字典的维护!
 * 其实就是下拉字典维护界面!! 只不过不可以新增新的类型!
 * 同时应该有一个标记是平台的字典,还是业务字典!! 然后业务字典还要有个排序!!!
 * @author xch
 *
 */
public class OtherBaseDataWKPanel extends AbstractWorkPanel {

	@Override
	public void initialize() {
		this.setLayout(new BorderLayout());
		ComboxDictManagerPanelNew ma = new ComboxDictManagerPanelNew(false);
		this.add(ma);
	}

}
